server:
  port: 8000

spring:
  application:
    name: gateway
  config:
    import: "optional:consul:"
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    consul:
      enabled: true
      discovery:
        query-passing: true
      config:
        data-key: application.yml
        prefix: config
        format: yaml
        default-context: defaults
    gateway:
      routes:
        - id: foodfetcher
          uri: lb://foodfetcher
          predicates:
            - Path=/api/food/**
        - id: emailservice
          uri: lb://emailservice
          predicates:
            - Path=/api/email/**
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins:
              -  "http://localhost:3000"
            allowed-headers:
              -  "*"
            allowed-methods:
              - "GET"
              - "POST"
              - "OPTIONS"
            allow-credentials: true

